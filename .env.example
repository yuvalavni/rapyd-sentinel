# Copy to GitHub Actions secrets / variables. Never commit real keys.
# AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY are bootstrap-only
# (first apply of OIDC + state backend). Main pipeline uses OIDC.

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_ACCOUNT_ID=
AWS_REGION=us-east-2
